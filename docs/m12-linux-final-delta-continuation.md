# M12 Linux final delta continuation

## Purpose and stopping rule

This is the current Linux entry point after the Windows round of 2026-09-21.
It narrows the generic procedures in the older native and cross-platform
continuations. Do not execute their complete manual checklists again.
Communicate with the operator in Spanish; write public evidence in English.

The Linux local physical delta is informational form presentation only. A small
SSH delta remains separate. Native artifact preparation and final automated
gates are agent work, not a request to repeat the operator's completed journey.
Once the applicable delta passes, close its row. Do not start another platform
cycle because documentation, test harnesses or evidence commits changed.

## Repository and source identity

- Repository: https://github.com/fpinero/relayterm.
- Delivery branch: `fix/m12-windows-final-candidate`.
- Windows evidence checkpoint: `e4431f7b357fe1388963fa94675a567875492fc3`.
- Product candidate: `691a8fbb45980658b98d647a85ea8305b2325938`.
- Completed Linux correction: `15794ad6e7f9459c89c512e679f5648bf9a3d231`.
- Earlier Linux journey: `9cf91f7164235d35beda81d9c8c2d4200b703aad`.

Inspect the working tree first, preserve all local changes and fetch origin.
Verify ancestry rather than assuming the local branch is current. Use a suitable
Linux work branch, never main/master. Inspect any later remote changes before
adopting them. Do not reset, stash, overwrite or silently commit existing work.
The delivery includes test-harness changes in e23be95, not new product behavior.
Build the identified product source in isolated clean checkouts and execute the
corrected harness from the delivery branch against the exact installed binary.
Keep harness commit, product commit and artifact hashes separate.

Read AGENTS.md, applicable local instructions, PROJECT_VISION.md,
MVP_TECHNICAL_SPEC.md, README.md, TODO.md, the latest avances.md entries,
M12_details.md, m12-linux-final-report.md, m12-linux-manual-observations.md,
m12-macos-correction-candidate.md, m12-windows-final-report.md,
release-builds.md and the checked-in Quality/Security workflows. Paths without
a directory in this paragraph refer to docs, except root project files.

## Evidence to reuse without another manual journey

| Completed Linux observation | Evidence boundary and reuse |
| --- | --- |
| Session identities, order, duplicate/cleared names, detach/reattach, reopen | 9cf91f7 native journey; no relevant subsequent production change |
| Unicode editing, cursor editing, resize, minimum-size screens and recovery | 9cf91f7; retain the documented code-point and full-Help limits, do not reopen the whole matrix |
| Competing input, both writer transfers, live dimensions and fresh wrapping | 9cf91f7 native and SSH; unaffected by typed form feedback |
| Claim, progress, handover, successor, completion and persistence | 9cf91f7 with independent readback; do not repeat manually |
| Backup, restore, stable records and honest lost sessions | 9cf91f7; preserve backup/home and empty-worktree fixture limitation |
| Release/repeated release, disconnected non-writer labels, no Events jump, stable diagnostics and normal exit | Physical 15794ad correction round completed; do not repeat locally |
| Stale rename, separate review/adoption, updated context, draft retention and safe discard | Physical 15794ad with revision-5 readback; only new Info presentation needs local observation |
| SSH survival, reconnection identity and normal exit | 9cf91f7; reuse continuity/size evidence, test only later presentation changes |

The production delta from 15794ad to 691a8fb is typed in-memory form feedback:
informational guidance uses Info, genuine failures retain Error. Protocol,
schema, lease ownership, writer size and explicit submission are unchanged.
Verify this diff before relying on the mapping. A new behavioral change can
justify an affected retest only if its concrete impact is documented first.

## Autonomous preparation

1. Inventory retained Linux artifacts and logs before building. Reuse complete
   evidence for the exact source and declared inputs if it exists. The known
   double-built 9cf91f7 archive and copied 15794ad executable do not establish
   final 691a8fb packaging; do not relabel their hashes.
2. If final native artifacts are absent, follow release-builds.md and script
   help: two clean native builds, compare build records, two packages, compare
   manifests, verify external manifest/archive checksums before extraction,
   inspect the nine-member inventory, ELF interpreter/dependencies and glibc
   symbol floor, then extracted smoke. Use pinned Rust, locked dependencies,
   documented production features and separate fresh output directories.
3. Install with the packaged helper in a dedicated user-local location. Verify
   the installed hash, version, help and actual Bash resolution. Preserve all
   earlier installs, state and backups. Record source, target, OS/runtime,
   toolchain, sizes, executable/archive hashes and signing state.
4. With RELAYTERM_TEST_RT set to that exact installed executable, execute the
   documented session_presentation, tui_gate, worktree_gate and backup_restore
   gates. Keep fixture binaries distinct from the installed production binary.
   Check every exit status; pipeline logging must not mask failures.
5. Reconcile required source, security and native CI checks with existing
   final-source evidence. Consult the delivery report for current workflow links
   and inspect their conclusions. Do not dispatch or rerun complete hosted
   suites merely because a documentation-only handoff commit follows a tested
   ancestor. Required independent automated repetitions are not duplicate
   physical journeys. Preserve failures and distinguish local debug, release
   and hosted measurements without raising fixed budgets.
6. Assess independent clean-runtime proof using available authorized isolation.
   A developer host with a filtered PATH is insufficient. Prefer an existing
   disposable runtime without build tools. Automate installation, discovery,
   daemon/PTY smoke and applicable quick-start commands. Reuse unchanged manual
   interaction evidence explicitly; do not ask for the whole visual quick start
   again. If isolation is unavailable, record the exact missing prerequisite
   as a bounded pending gate rather than sending the operator around the loop.

## Only local physical delta

Prepare the synthetic fixture and both clients before requesting operator input.
Use the final installed binary and a small private workspace. Explain that the
conflict is setup for observing the new Info rendering, not a reopened conflict
acceptance test. Guide one checkpoint at a time:

1. Keep a short rename draft open in A and save a different label from B. In A,
   submit the stale draft, then Ctrl-R to review and Ctrl-R to adopt. Observe
   the new Info prefix and explicit-submission guidance, readable wrapping,
   visible retained cursor/draft and updated current label. Independently verify
   adoption did not change the winner or revision; discard safely.
2. Submit an empty task title. Observe Error: Title is required., the still-open
   form and visible title cursor. Verify independently that no task was created,
   then cancel. This closes the Info versus real validation Error contrast.

Do not request another six-screen, writer-size, handover, backup, disconnect or
20-second local diagnostic round. Do not require duplicate images where one
operator confirmation and the necessary independent readback establish the row.

## Bounded SSH delta

First inspect the latest Linux evidence and existing authorized connection.
If later evidence already covers the 15794ad/691a8fb presentation changes over
SSH, reuse it and omit this section's execution. Otherwise, use one connection
and one small synthetic fixture to cover changed rename review/context/Info,
changed disconnected navigation/non-writer feedback, repeated release without
an Events jump, diagnostic stability, normal exit and reconnection identity.
Automate setup and independent reads; request only the actual remote-terminal
observations that automation cannot establish. Reuse completed SSH input-size,
Unicode, child-survival and handover evidence.

Do not repeat deliberate SSH-client SIGTERM to rediscover the known recovery
limitation. The minimal SSH-only control already reproduced missing restoration
bytes without Relayterm. Preserve the failed automatic restoration result and
documented local reset recovery. Likewise, fixed-grid historical output does
not reflow; fresh command wrapping already passed. Reconcile both limits with
the acceptance contract; neither another screenshot nor relabelling resolves a
maintainer acceptance decision.

Use existing destination authorization when it covers disposable test processes.
If it does not, obtain narrowly scoped authorization before terminating them.
Do not modify SSH services, authentication, firewall or security configuration
without explicit authorization. If the connection is unavailable, identify that
single blocker, complete independent work and retain the SSH row as pending.

## Completion and evidence contract

Keep screenshots, transcripts, databases, raw logs and private paths outside Git.
Record actual outcomes only, with code, artifact, automated and physical evidence
clearly distinguished. Append to avances.md without changing old entries, remove
only verified tasks from TODO.md, and update the Linux report and matrices.
Preserve the original failed observations. Do not rename old hashes as new proof.

Finish with a focused local commit and a finite list of remaining requirements,
their owner/environment and exact missing proof. Do not request another Windows
or macOS visual round for this unchanged product candidate. Do not start M13 or
declare global M12 completion while Linux/SSH, independent clean-runtime,
performance/CI or acceptance requirements remain open. This continuation does
not authorize push, merge, tags or releases on the Linux destination.
